# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    name:
        label: Name
        searchable: true

    client_id:
        label: Client Id
        searchable: true

    client_secret:
        label: Client name
        searchable: true

    workspace:
        label: Workspace
        searchable: true